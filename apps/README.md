// Wails + Vite(React/TS) 앱 스캐폴딩 필요
// Wails CLI로 아래 명령 실행 필요:
//
// wails init -n app-file-controller -t react-ts
// wails init -n app-decg-link -t react-ts
//
// 또는 수동으로 apps/app-file-controller, apps/app-decg-link에서 각각 실행
